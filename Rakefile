this_dir = File.dirname(__FILE__)

task :default => ['samples_project_testing:copy_dependencies'] do
end

namespace "samples_project_testing" do
  load "#{this_dir}/samples-project-testing/Rakefile"
end
