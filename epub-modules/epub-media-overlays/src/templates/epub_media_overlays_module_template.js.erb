var EpubMediaOverlaysModule = function (arguments_to_initialize_module) {

    var EpubMediaOverlays = {};

    // Rationale: The order of these matters
    <%= name_of_javascript_file_specified_in_rakefile %>

    var module_entry_point = new EpubMediaOverlays.MediaOverlaysControllerOrWhatever({
    });

    // Description: The public interface
    return {

        api_method : function () {
            return module_entry_point.method_to_call();
        }
    };
};
